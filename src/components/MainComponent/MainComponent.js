import React from 'react';
import ReactDOM from 'react-dom';

const MainComponent = (props) => (
  <div>
    <AppBar title="Github Finder" showMenuIconBtn={false} />
  </div>
)

export default MainComponent;
